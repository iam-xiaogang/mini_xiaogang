<view class="container">
  <view wx:if="{{functionType === 'pdfToWord'}}" class="pdf-word-wrapper">
    <pdf-to-word />
  </view>
  <view wx:if="{{functionType === 'pdfadd'}}" class="pdf-word-wrapper">
    <pdfadd />
  </view>
  <view wx:if="{{functionType === 'img_to_word'}}" class="pdf-word-wrapper">
    <img_to_word />
  </view>
  <view wx:else>
  </view>
  

</view>
